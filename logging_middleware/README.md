# Logging Middleware

This folder contains the reusable logging helper used across the project.

The middleware is responsible for sending structured logs to the evaluation server instead of relying on console logging. It keeps log calls consistent across backend and frontend code.

## Run

```bash
npm install
npm test
```
